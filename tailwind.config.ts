/** @type {import('tailwindcss').Config} */
export default {
  darkMode: 'class', // ← allows toggling manually
  content: ['./src/**/*.{html,js,svelte,ts}'],
  theme: {
    extend: {},
  },
  plugins: [],
}

